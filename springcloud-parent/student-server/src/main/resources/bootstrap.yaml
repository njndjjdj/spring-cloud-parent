
spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        shared-configs:
          - dataId: common.yml
            refresh: true
          - data-id: student-server.yml
            group: STUDENT_GROUP
            refresh: true
        file-extension: yml